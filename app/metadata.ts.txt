import type { Metadata, Viewport } from "next";

/**
 * Drop these into app/layout.tsx (App Router). They wire the PWA manifest,
 * the official app icons, and the ink theme-color for the mobile status bar.
 */
export const metadata: Metadata = {
  title: "hertz PR Hub",
  description: "CRM operativo per PR e capi promoter — hertz",
  manifest: "/manifest.webmanifest",
  appleWebApp: {
    capable: true,
    title: "hertz PR",
    statusBarStyle: "black-translucent",
  },
  icons: {
    icon: [
      { url: "/icons/icon-192.png", sizes: "192x192", type: "image/png" },
      { url: "/icons/icon-512.png", sizes: "512x512", type: "image/png" },
    ],
    apple: [{ url: "/icons/apple-touch-icon.png", sizes: "180x180" }],
  },
};

export const viewport: Viewport = {
  themeColor: "#151515",
  width: "device-width",
  initialScale: 1,
  viewportFit: "cover", // respect the iOS safe-area (notch / home indicator)
};
